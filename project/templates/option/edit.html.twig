{% extends 'base.html.twig' %}

{% block title %}Modifier une Option{% endblock %}

{% block body %}
    <h1>Modifier une Option</h1>

    {{ include('option/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="d-flex justify-content-between mt-3">
        <a href="{{ path('app_option_index') }}" class="btn btn-dark">Retour à la liste</a>
        {{ include('option/_delete_form.html.twig') }}
    </div>
{% endblock %}
